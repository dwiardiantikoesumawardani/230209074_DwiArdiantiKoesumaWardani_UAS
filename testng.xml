<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">

<suite name="Demoblaze Automation Test Suite"
       parallel="false"
       verbose="1">

    <!-- ===================== -->
    <!-- LOGIN TEST SUITE -->
    <!-- ===================== -->
    <test name="Login Tests">
        <classes>
            <class name="com.demoblaze.tests.LoginTest"/>
        </classes>
    </test>

    <!-- ===================== -->
    <!-- ADD TO CART TEST SUITE -->
    <!-- ===================== -->
    <test name="Add To Cart Tests">
        <classes>
            <!-- Akan diaktifkan setelah test dibuat -->
            <class name="com.demoblaze.tests.AddToCartTest"/>
        </classes>
    </test>

    <!-- ===================== -->
    <!-- CHECKOUT / PLACE ORDER TEST SUITE -->
    <!-- ===================== -->
    <test name="Checkout Tests">
        <classes>
            <!-- Akan diaktifkan setelah test dibuat -->
            <!--class name="com.demoblaze.tests.CheckoutTest"/ -->
        </classes>
    </test>

</suite>
